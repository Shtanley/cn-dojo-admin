import { students } from "./data";

export const load = async () => {
    /**
     * Fetch students
     */
    return {
        students
    }
};

export const actions = {
    add: () => {},
    update: () => {},
    remove: () => {}
};