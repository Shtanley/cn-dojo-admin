import { students } from "./data";

export const load = async ({params}) => {
    return {
        students
    }
};

export const actions = {
    add: () => {},
    update: () => {},
    remove: () => {}
};