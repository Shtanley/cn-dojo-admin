export const load = async () => {
    /**
     * Fetch students
     */
    let products = [
        {
            name: "Jellybean"
        }
    ]
    return {
        products
    }
};

export const actions = {
    add: () => {},
    update: () => {},
    remove: () => {},
};