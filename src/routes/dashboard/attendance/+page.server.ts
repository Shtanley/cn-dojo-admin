import { students } from "../students/data";


export const load = async () => {
    /**
     * Fetch students
     */
    return {
        students
    }
};

export const actions = {
    add: () => {},
    remove: () => {},
};